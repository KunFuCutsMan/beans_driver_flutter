import 'package:flutter/material.dart';

class ViewSirveServicio extends StatelessWidget {
  const ViewSirveServicio({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Sirve servicio"));
  }
}