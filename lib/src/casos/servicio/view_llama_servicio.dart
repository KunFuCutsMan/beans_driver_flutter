import 'package:flutter/material.dart';

class ViewLlamaServicio extends StatelessWidget {
  const ViewLlamaServicio({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Pide servicio"));
  }
}